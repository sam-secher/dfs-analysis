name: dfs-analysis
channels:
  - conda-forge
  - https://conda.anaconda.org/gurobi
  - defaults
dependencies:
  - requests
  - python
  - pandas
  - types-requests
  - streamlit
  - pandas-stubs
  - plotly
  - scikit-learn
  - blas[build=*openblas]
  - libblas[build=*openblas]
  - libcblas[build=*openblas]
  - liblapack[build=*openblas]
  - numpy
  - scipy
  - statsmodels
prefix: C:\Users\sam.secher\AppData\Local\anaconda3\envs\dfs-analysis
